package com.sinse.shopadmin.product.repository;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

// javaEE 개발에서는 개발자들 간에 협업을 위해 정해진 규칙에 따라 코드를 작성해야 함..
// 특히 DB에서 테이블이 하나 정의되면 java 개발자는 이 테이블에 대해 1:1 대응하는 모델 객체와 
// 이 테이블에 대한 CRUD(C = insert, Read=Select, Update, Delete)를 무조건 만든다. 
// 예를 들어, 협업관계에 있는 같은 팀 프로젝트 구성원인 A, B가 있다고 가정했을 때, Product 테이블에 대해서 A가 모델, DAO를 만들면 B는 그 객체를 가져가 사용하면 됨.. 중복 정의 X
// 오직 데이터베이스에 관련된 코드만 들어있어야 하며, Swing을 제어하는 등의 디자인코드가 절대로 들어있으면 안됨
// 이 객체는 MVC영역 중 Model 영역, 즉 로직 객체 영역을 담당하므로 view는 들어가면 안됨
public class TopCategoryDAO { // 이 DAO는 관리자용이 아님... 누구나 가져다 쓸 수 있게 만들어야 함
	
	// TopCategory의 모든 레코드 가져오기
	public void selectAll() {
		
		Connection conn = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		
		StringBuffer sql = new StringBuffer();
	}
	
	// TopCategory의 레코드 한 건 가져오기
	public void select() {
		
	}
	
	// 레코드 한 건 입력
	public void insert() {
		
	}
	
	// 레코드 한 건 수정
	public void update() {
		
	}
	
	// 레코드 한 건 삭제
	public void delete() {
		
	}
	
}
